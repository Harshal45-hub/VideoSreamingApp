/**
 * @format
 */

import { AppRegistry } from 'react-native';
import AuthStack from './frontend/AuthStack';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => AuthStack);
